steps:
  # Single step to merge to main branch
  - name: 'gcr.io/cloud-builders/git'
    entrypoint: 'bash'
    args:
      - '-c'
      - |
        git checkout main &&
        git merge qa &&
        git push origin main